sudo: false
language: php
php:
  - '7.3'
  - '7.4'
  - '8.0'
  - '8.1'
cache:
  directories:
    - $HOME/.composer/cache
before_script:
  - composer install --prefer-source --no-interaction
  - mkdir -p build/logs
script:
  - XDEBUG_MODE=coverage vendor/bin/phpunit
